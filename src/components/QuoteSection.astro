<section class="quote-section">
  <div class="quote-shell">
    <p class="quote-copy">
      "Creating high-quality bathroom furniture from solid wood - that is our passion, that is where our heart and soul lie!"
    </p>
    <div class="quote-author">
      <span class="quote-name">Reiner Brenner</span>
      <span class="quote-role">joiner</span>
    </div>
  </div>
</section>

<style>
.quote-section {
  background: linear-gradient(180deg, var(--warm-white) 0%, var(--cream) 100%);
  border-top: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
  padding: 6rem 5rem;
}

.quote-shell {
  max-width: 980px;
  margin: 0 auto;
}

.quote-copy {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(2rem, 4.2vw, 3.6rem);
  line-height: 1.08;
  color: var(--wood-dark);
  margin-bottom: 2rem;
  text-wrap: balance;
}

.quote-author {
  display: flex;
  align-items: baseline;
  gap: 1rem;
  color: var(--text-sub);
}

.quote-name {
  font-family: 'Cormorant Garamond', serif;
  font-size: 1.3rem;
  color: var(--wood-accent);
}

.quote-role {
  font-size: 0.72rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
}

@media (max-width: 900px) {
  .quote-section {
    padding: 4.5rem 2rem;
  }
}

@media (max-width: 600px) {
  .quote-section {
    padding: 3.5rem 1.25rem;
  }

  .quote-author {
    flex-direction: column;
    align-items: flex-start;
    gap: 0.35rem;
  }
}
</style>
